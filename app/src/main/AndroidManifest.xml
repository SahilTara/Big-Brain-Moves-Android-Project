<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uottawa.bigbrainmoves.servio">
    xmlns:tools="http://schemas.android.com/tools"
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activites.SplashScreenActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activites.MainActivity" />
        <activity android:name=".activites.LoginOrSignUpActivity" />
        <activity android:name=".activites.SignUpActivity" />
        <activity android:name=".activites.ServiceMainActivity" />
        <activity android:name=".activites.AdminMainActivity" />
        <activity android:name=".activites.UserListActivity" />
        <activity android:name=".activites.ManageServiceTypesActivity" />
        <activity android:name=".activites.CreateServiceTypeActivity"></activity>
    </application>

</manifest>