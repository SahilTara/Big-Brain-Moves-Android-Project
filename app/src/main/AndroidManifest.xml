<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uottawa.bigbrainmoves.servio">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.SplashScreenActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.LoginOrSignUpActivity" />
        <activity android:name=".activities.SignUpActivity" />
        <activity android:name=".activities.ServiceMainActivity" />
        <activity android:name=".activities.AdminMainActivity" />
        <activity android:name=".activities.UserListActivity" />
        <activity android:name=".activities.ManageServiceTypesActivity" />
        <activity android:name=".activities.CreateServiceTypeActivity" />
    </application>

</manifest>